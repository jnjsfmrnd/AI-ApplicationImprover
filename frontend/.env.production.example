# Production frontend env — used during `npm run build` for Azure deployment
# Replace with your actual App Service URL after running `azd provision`
VITE_API_BASE_URL=https://<your-backend-appservice>.azurewebsites.net/api
